{
  "version": 2,
  "buildCommand": "if [ -d \"../../packages/types\" ]; then cd ../..; fi && npx tsc -p packages/types/tsconfig.json && npx vite build apps/web",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
